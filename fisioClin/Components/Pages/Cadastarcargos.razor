@page "/CadastrarCargos"


@code {

}
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastrar Cargos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f2f0f0;
            font-family: 'Poppins', sans-serif;
        }

        .form-container {
            background-color: #fff;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            max-width: 800px;
            margin: 60px auto;
        }

        h2 {
            color: #6b0a0a;
            text-align: center;
            margin-bottom: 30px;
            font-weight: 600;
            text-decoration: underline;
        }

        .btn-custom {
            background-color: #6b0a0a;
            color: white;
            border: none;
            transition: 0.3s;
            padding: 10px 30px;
            border-radius: 10px;
        }

            .btn-custom:hover {
                background-color: #8a0f0f;
                color: #fff;
            }

        label {
            font-weight: 500;
        }
    </style>
</head>
<body>

    <div class="form-container">
        <h2>Cadastrar Cargos</h2>
        <form>
            <div class="mb-3">
                <label for="nomeCargo" class="form-label">Nome do cargo</label>
                <input type="text" class="form-control" id="nomeCargo" placeholder="Digite o nome do cargo">
            </div>

            <div class="mb-3">
                <label for="departamento" class="form-label">Departamento</label>
                <input type="text" class="form-control" id="departamento" placeholder="Informe o departamento">
            </div>

            <div class="mb-3">
                <label for="descricao" class="form-label">Descrição do cargo</label>
                <textarea class="form-control" id="descricao" rows="3" placeholder="Descreva as atividades do cargo"></textarea>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label for="cargaHoraria" class="form-label">Carga horária semanal</label>
                    <input type="number" class="form-control" id="cargaHoraria" placeholder="Ex: 40">
                </div>

                <div class="col-md-6 mb-3">
                    <label for="dataCriacao" class="form-label">Data de criação</label>
                    <input type="date" class="form-control" id="dataCriacao">
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label for="dataAtualizacao" class="form-label">Data última atualização</label>
                    <input type="date" class="form-control" id="dataAtualizacao">
                </div>

                <div class="col-md-6 mb-3">
                    <label for="observacoes" class="form-label">Observações</label>
                    <input type="text" class="form-control" id="observacoes" placeholder="Digite observações">
                </div>
            </div>

            <div class="text-end">
                <button type="submit" class="btn btn-custom">Cadastrar</button>
            </div>
        </form>
    </div>

</body>
</html>
