@page "/laudomedico"

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Laudo Médico</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="style.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="container py-4">
        <h4 class="text-danger fw-bold mb-3">laudo médico</h4>

        <div class="row align-items-center mb-4">
            <div class="col-md-2">
                <input type="text" class="form-control text-center" value="234" readonly>
            </div>
            <div class="col-md-4">
                <select class="form-select">
                    <option selected>Roberto Castellan</option>
                </select>
            </div>
        </div>

        <div class="row g-4">
            <!-- Coluna esquerda -->
            <div class="col-md-6">
                <div class="card shadow-sm border-0 h-100">
                    <div class="card-body">
                        <h5 class="fw-bold text-danger">Laudo <span class="text-muted fs-6">VÁLIDO PARA DOIS ANOS</span></h5>

                        <div class="mt-3">
                            <h6 class="fw-bold">TIPO DE EXAME</h6>
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="exame" id="motora" checked>
                                <label class="form-check-label" for="motora">Reabilitação Motora</label>
                            </div>
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="exame" id="respiratorio">
                                <label class="form-check-label" for="respiratorio">Fisioterapia Respiratória</label>
                            </div>
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="exame" id="pilates">
                                <label class="form-check-label" for="pilates">Pilates</label>
                            </div>
                        </div>

                        <div class="mt-4">
                            <h6 class="fw-bold">DIAGNÓSTICO</h6>
                            <p class="mb-1">Paciente diagnosticado com escoliose.</p>
                        </div>

                        <div class="mt-3">
                            <h6 class="fw-bold">OBSERVAÇÕES</h6>
                            <p class="text-muted small">
                                Paciente com escoliose leve. Refere dor leve na região lombar, principalmente após longos períodos em pé.
                                Postura com discreta assimetria de ombros e pelve. Sem limitações importantes de movimento. Iniciado
                                tratamento com alongamentos, fortalecimento de musculatura paravertebral e orientações posturais.
                                Reavaliação em 4 semanas.
                            </p>
                        </div>

                        <div class="mt-4 d-flex justify-content-between align-items-center">
                            <span class="fw-bold">STATUS: <span class="text-danger">EM ABERTO</span></span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Coluna direita -->
            <div class="col-md-6">
                <div class="card shadow-sm border-0 h-100">
                    <div class="card-body text-center">
                        <h5 class="fw-bold text-danger mb-4">Imagens e anexos</h5>
                        <img src="https://cdn.pixabay.com/photo/2017/09/04/18/41/back-2718825_960_720.jpg"
                             class="img-fluid rounded mb-3" alt="Fisioterapia">
                        <div class="d-flex align-items-center justify-content-center gap-2 text-danger">
                            <i class="bi bi-folder-plus fs-4"></i>
                            <span class="fw-semibold">adicionar anexo</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="text-end mt-4">
            <button class="btn btn-danger px-4 py-2 rounded-pill fw-bold">
                <i class="bi bi-printer-fill me-2"></i>Emitir laudo
            </button>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.js"></script>
</body>
</html>

<style>
    body {
        font-family: "Poppins", sans-serif;
    }

    .card {
        border-radius: 15px;
    }

    .text-danger {
        color: #8B0000 !important;
    }

    .btn-danger {
        background-color: #8B0000;
        border: none;
    }

        .btn-danger:hover {
            background-color: #a10e0e;
        }

    input[readonly] {
        background-color: #f8f9fa;
        border: 1px solid #ccc;
    }

    .form-check-label {
        font-size: 0.95rem;
    }

    .small {
        font-size: 0.9rem;
    }

</style>